[build]
  base = "."
  command = "cd frontend && npm run build"
  publish = "frontend/.next"
  functions = "netlify/functions"

[build.environment]
  NODE_VERSION = "18"
  NPM_VERSION = "9"

# API redirects to serverless functions
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/api"
  status = 200

# SPA routing - redirect all routes to Next.js
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
